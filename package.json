{
  "name": "babulfish-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm run test:scripts && pnpm -r run test",
    "test:scripts": "node --test scripts/*.test.mjs",
    "lint": "pnpm -r run lint",
    "eval:webgpu": "node scripts/webgpu-eval.mjs",
    "consumer:smoke": "node scripts/consumer-smoke.mjs",
    "docs:check": "pnpm --filter babulfish lint && pnpm --filter @babulfish/styles lint && pnpm run consumer:smoke",
    "release:check": "pnpm build && pnpm test && pnpm docs:check",
    "release": "pnpm run release:check && node scripts/publish-public-packages.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
