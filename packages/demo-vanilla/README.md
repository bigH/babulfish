# @babulfish/demo-vanilla

Zero-framework translation demo — proves that `@babulfish/core` works with
pure DOM APIs and no UI framework. Uses Vite as a dev server.
Run `pnpm --filter @babulfish/demo-vanilla dev` to start locally.
See the [root README "Pick your binding" matrix](../../README.md) for other demos.
